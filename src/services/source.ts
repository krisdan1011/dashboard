
import Source from "../models/source";

namespace source {
    export function createSource(source: Source) {
        console.log(source);

        // Create the source
    }
}

export default source;